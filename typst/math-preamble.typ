// Shared definitions for math_engine: typst pages.
//
// Keep document/page layout settings in scripts/render_typst_math.py. Add
// reusable symbols and math helpers here; this file participates in the SVG
// cache key, so changing it automatically rebuilds affected formulas.
